pipeline {
    agent any

    stages {
        stage('Aws') {
            agent {
                docker {
                    image 'amazon/aws-cli'
                    args '--entrypoint ""'
                    reuseNode true
                }
            }
            steps {
                sh '''
                    aws --version
                '''
            } 
        }
    }
}